# genoio

`genoio` is a Python-first genotype IO package with a Rust parser backend.

Phase 1 provides the package skeleton, source resolution, structured public
errors, and lightweight dataset handles. Parser implementations are added in
later phases.
